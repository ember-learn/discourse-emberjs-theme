{{! manually lifted from https://github.com/ember-learn/ember-styleguide/ }}

<div class="footer-info container">
  <div class="footer-info-links">

    {{d-icon "ember-logo" class="footer-logo"}}

    <div class="spacer"></div>

    {{#each @infoLinks as |link|}}
      <a href={{link.href}} class="info-link">{{link.name}}</a>
    {{/each}}
  </div>
  <div class="footer-social hide-on-mobile">
    {{#each @socialLinks as |link|}}
      <a
        href={{link.href}}
        title={{link.title}}
        aria-label={{link.label}}
        rel="me"
      >
        {{d-icon link.class class="bi"}}
        {{link.title}}
      </a>
    {{/each}}
  </div>
</div>